{
  "name": "clean-node",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --silent --colors --noStackTrace --passWithNoTests",
    "test:unit": "jest --watch --silent --colors --noStackTrace --passWithNoTests -c jest-unit-config.js",
    "test:integration": "jest --watch --silent --colors --noStackTrace --passWithNoTests -c jest-integration-config.js",
    "test:staged": "jest --passWithNoTests --findRelatedTests --silent --noStackTrace",
    "test:ci": "jest --coverage --silent --colors --noStackTrace --passWithNoTests"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "keywords": [
    "lint",
    "git",
    "staged",
    "eslint",
    "prettier"
  ],
  "author": "Fernando Marca Magalhães",
  "license": "ISC",
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "pre-push": "npm run test:ci"
    }
  },
  "lint-staged": {
    "src/**/*.js": [
      "standard --fix",
      "npm run test:staged",
      "git add"
    ]
  },
  "devDependencies": {
    "husky": "^6.0.0",
    "jest": "^27.0.4",
    "lint-staged": "^11.0.0",
    "standard": "^16.0.3"
  },
  "dependencies": {
    "validator": "^13.6.0"
  }
}
